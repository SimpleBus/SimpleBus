services:
    public_message_serializer:
        alias: simple_bus.jms_serializer.message_serializer

    # needed to make JMSSerializerBundle work
    translator:
        class: stdClass

    # needed to make JMSSerializerBundle work
    annotation_reader:
        class: Doctrine\Common\Annotations\AnnotationReader